# Core Rendering
# Pinned to >=5.9 since that's the version range this migration's API
# usage (Texture.get_handle, ComputeShader.run, Buffer format strings)
# was actually verified against -- "latest" could drift to something
# with different behavior later without anyone noticing.
moderngl>=5.9
glfw==2.10.0

# Computation
numpy==2.2.3

# Image loading
Pillow==11.2.1
